<Window x:Class="WpfApp20.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp20"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox x:Name="carListBox" Grid.Row="0" Margin="10" SelectionChanged="CarListBox_SelectionChanged"></ListBox>


        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">

            <Button Content="Add" Click="AddButton_Click" FontSize="25" Margin="5"></Button>
            <Button Content="Delete" Click="DeleteButton_Click" FontSize="25" Margin="5"></Button>
            <Button Content="Search" Click="SearchButton_Click" FontSize="25" Margin="5"></Button>
            <TextBox x:Name="searchTextBox" Width="200" FontSize="35" Margin="5"></TextBox>

        </StackPanel>


    </Grid>
</Window>
